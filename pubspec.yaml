name: elegant_toast
description: A beautiful, customizable toast notification package for Flutter with success, error, warning, info, and neutral variants.
version: 1.0.0
homepage: https://github.com/rasel2510/elegant_toast

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
