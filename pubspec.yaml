name: elegant_toast
description: A beautiful, customizable Flutter toast package with progress bar, action buttons, toast queue, swipe to dismiss, persistent toasts, and loading toasts.
version: 1.0.3
homepage: https://github.com/Rasal2510/elegant_toast
repository: https://github.com/Rasal2510/elegant_toast
issue_tracker: https://github.com/Rasal2510/elegant_toast/issues

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
