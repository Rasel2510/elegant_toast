/// The entrance/exit animation style for the toast.
enum ToastAnimation {
  /// Slide in from top or bottom + fade. Default.
  slideAndFade,

  /// Fade in/out only.
  fade,

  /// Scale up from center + fade.
  scale,

  /// Bounce effect on entry.
  bounce,
}
